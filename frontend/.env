# ⚠️  La clave de Google Maps está expuesta al navegador (todas las keys de
# Maps lo están). DEBE restringirse por referer/quota en Google Cloud Console.
# Esta clave está committeada en el repo — rotarla antes de desplegar a prod.
VITE_GOOGLE_MAPS_KEY=AIzaSyCFqw9StC-U_rkSzAA-VEFrv-J7e-adLms

# Backend API
VITE_API_URL=http://localhost:8000

# Realtime — laravel-echo-server (protocolo socket.io) expuesto en :6001
VITE_ECHO_HOST=http://localhost:6001

# Reservados para futura migración a Laravel Reverb / Pusher (no usados hoy)
VITE_PUSHER_APP_KEY=local
VITE_PUSHER_APP_CLUSTER=mt1
